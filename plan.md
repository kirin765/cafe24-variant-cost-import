# 옵션별 공급가 CSV 가져오기 — 구현 계획

- 작성일: 2026-09-16
- 프로젝트: `cafe24-variant-cost-import` — 후보 4
- 상태: A. 2시간 데모 구현·자동 검증 완료(2026-09-16). B. OAuth 콜백·토큰 교환·launch 서명·Cafe24 상품목록 CSV adapter·자동 검증(2026-09-17). 품목 쓰기 검증·C 미착수
- 담당: Giwan(제품 선택·테스트몰 검증), 개발 세션(구현·자동 검증)

## 1. 목적과 근거

**공급사 원가표를 Cafe24 품목에 매칭하고 변경점을 검토한 뒤 옵션별 공급가만 수정한다.** 판매가 예약·재고 수정과 다른 작업이다.

2026-09-16 연구에서 [공식 6월 10일 공지](https://shopnotice.cafe24.com/view?bbs_no=12&no=348879)는 품목 공급가의 Excel 업로드 수정을 제외한다고 설명했다. [Admin API 문서](https://developers.cafe24.com/docs/en/api/admin/)에는 품목 수정의 `supply_price`가 확인됐다. 문서 근거이며 실제 쓰기 검증은 아니다. 직접 고객 요청·결제 증거는 아직 없다.

근거: [후보 비교](../llm-wiki/reports/cafe24-broad-experiment-candidates-2026-09-16.md), [원자료](../llm-wiki/raw/cafe24-broad-candidates-2026-09-16/catalog-notes.md).

## 2. 범위

### 첫 데모 — 2시간, 추가 현금 0원 제안

- 합성 Cafe24 품목 목록과 공급가 CSV를 불러온다.
- 품목 코드를 매칭하고 변경 전/후 공급가·오류를 표시한다.
- 중복·미매칭·잘못된 숫자를 차단한다.
- 검토 결과와 변경 명세를 파일로 내보낸다.
- 실제 API 쓰기·로그인·DB 없이 수행한다.

### 첫 연동 MVP

- 한 몰의 품목 조회, CSV 검증, 변경 미리보기, 명시적 확정.
- 확정한 작업을 서버에서 실행하고 행별 결과·변경 전 값을 보관.
- 실패/결과 불명 행을 구분하고 재조회 후 재시도.
- 변경 전 값으로 복원하는 별도 검토 작업.

### 제외

- 판매가·할인·재고·공급가 관리 설정 자동 변경.
- 상품명 유사도에 의한 자동 쓰기, 공급사 파일 전 형식 자동 해석.
- 예약/반복 실행, 외부 ERP 양방향 동기화.
- 원가 회계·세금·환율 자동 계산. 초기에는 몰 기본통화와 일치하는 파일만 사용.

새 독립 앱과 보유 수익관리 앱의 기능 확장 중 어느 형태가 맞는지는 실제 반복 사용·지불 의사를 보고 결정한다.

## 3. CSV와 매칭 규칙

```csv
variant_code,supply_price
SAMPLE_VARIANT_A,4500
SAMPLE_VARIANT_B,6200
```

샘플 코드는 합성 데이터이며 실제 코드 형식이 아니다.

Cafe24 관리자에서 내려받는 **상품목록 내보내기**는 `상품코드`·`공급가` 열을 가진 상품 단위 파일이다.
헤더로 형식을 판별해 이 파일도 읽는다. `상품코드`를 매칭 키로 쓰고 `자체 상품코드`는 쓰지 않는다
(실데이터에서 비어 있음). `공급가`의 `4500.00`은 소수점 이하가 0일 때만 정수로 정규화한다. 이 내보내기에는
옵션/품목 코드가 없어 옵션별 공급가는 옵션·품목 내보내기나 API 조회가 필요하다(2026-09-17, onnurimun 3개 파일 확인).

- 초기에는 플랫폼 품목 코드 정확 일치만 지원. 상품명·옵션명은 검토 화면의 참고 정보.
- 자체 품목 코드 지원은 tenant 내 유일성을 확인한 경우에만 후속 추가.
- 코드 앞자리 0과 문자열을 보존한다. 코드 내부 공백·문자 변형을 자동 교정하지 않는다.
- 필수값 누락, 파일 내 동일 코드 중복은 값이 같아도 오류.
- 빈 공급가는 0으로 해석하지 않는다. 0은 명시적 입력으로 허용하되 눈에 띄게 표시.
- 음수·NaN·통화 기호·모호한 천 단위 표기·과도한 정밀도는 오류.
- 첫 데모는 KRW 정수 원 단위. 실연동에서 API의 정밀도·허용 범위를 검증하고 금액을 decimal string/정확한 수치로 처리.
- UTF-8/BOM, 인용부호를 지원하는 CSV parser 사용. 초기 파일 1MB/1,000행 제한 제안.
- 일부 오류가 있으면 전체 확정을 막고 수정 파일을 다시 받는다. 부분 행 선택 실행은 후속 기능.

## 4. 화면과 데이터

| 화면 | 필요한 동작 | 완료 조건 |
|---|---|---|
| `/demo` | 합성 목록·CSV 매칭 | 실제 쓰기 없이 결과 재현 가능 |
| `/imports/new` | 파일·대상 몰 확인 | 몰/통화가 명확하고 오류 행을 찾을 수 있음 |
| `/imports/[id]/preview` | 전후 값·변경/동일/오류 집계 | 확정할 행과 금액이 모호하지 않음 |
| `/imports/[id]` | 실행·행별 상태·결과 파일 | 실패와 결과 불명을 구분 |
| 복원 미리보기 | 이전 값·현재 값·충돌 확인 | 다른 변경을 덮어쓰지 않음 |

| 엔터티 | 주요 필드 |
|---|---|
| shops/credentials | tenant, mall/shop 식별자, 앱 전용 암호화 토큰 |
| import_jobs | tenant, 상태, 파일 hash, 미리보기 버전, 확정시각 |
| import_rows | 행 번호, product/variant 식별자, 이전/목표 금액, 조회시각, 결과 |
| attempts | 행 ID, 시도 번호, 결과 종류, 오류 코드, 시작/종료시각 |
| restore_jobs | 원작업 ID, 복원할 행, 현재값 검사 결과, 별도 확정 |

원가 데이터는 영업 정보다. 조회·다운로드·복원 모두 tenant 권한 검사, 원본 파일/토큰/원가를 일반 로그에 출력하지 않음. 보관기간과 작업 삭제 정책은 파일럿 전에 정한다.

## 5. 실행 설계

작업 상태: `draft → validated → confirmed → running → completed | partial_failure | needs_review`. 실행 후 취소는 이미 성공한 값을 자동 복구한다는 의미가 아니다. 중지는 새 행 처리를 멈추고 미확정 결과를 재조회한다.

1. 품목과 현재 공급가를 조회하고 미리보기 snapshot을 만든다.
2. 변경 없는 행은 `unchanged`로 분리한다.
3. 사용자 확정을 미리보기 버전/hash에 연결한다. 파일 수정 후에는 재검토가 필요하다.
4. 실행 직전 품목의 현재 값을 재조회한다. 미리보기의 이전 값과 다르면 충돌로 중지한다.
5. 허용된 `supply_price`만 보내고 판매가/재고 등 다른 필드는 넣지 않는다.
6. 응답 유실·timeout이면 바로 실패로 단정하거나 맹목 재시도하지 않는다. 현재 값을 읽어 목표값/이전값/다른값으로 분류한다.
7. 응답 후 재조회로 반영 여부를 확인하고 행별 결과를 저장한다.
8. 복원 시 현재 값이 이 작업의 목표값과 같을 때만 이전 값을 제안한다. 다르면 충돌로 남겨 수동 검토한다.

몰/품목별 실행 잠금으로 앱 내부 동시 작업을 막는다. 플랫폼에 조건부 쓰기 기능이 없다면 재조회와 쓰기 사이 외부 변경 경쟁은 완전히 제거할 수 없다. 이 제한을 표시하고 파일럿 동안 동시 원가 편집을 피하도록 안내한다. 다중 행 전체가 원자적으로 처리된다고 약속하지 않는다.

## 6. 구조와 Cafe24 확인 과제

- TypeScript + Next.js, 순수 CSV 검증·매칭 모듈과 `VariantGateway` adapter.
- 데모는 fixture gateway. 연동 단계는 Postgres 작업 ledger와 지속 가능한 worker.
- 긴 작업을 브라우저 연결이나 짧은 HTTP 함수 수명에 의존시키지 않는다.
- 처음은 한 품목씩 제한된 동시성으로 처리. 배치 쓰기는 부분 실패 응답을 검증한 뒤 도입.
- mise/lockfile/배포·worker 환경은 구현 시작 시 결정.

```text
src/features/imports/   parse, validate, match, preview
src/features/jobs/      상태 전이, 재시도, 복원
src/lib/cafe24/         OAuth·VariantGateway
src/workers/            실행·재조회·복구
src/fixtures/           합성 품목·오류 CSV
db/migrations/          작업·행·시도 이력
QA.md                   수동 검증표
```

- [ ] 공식 문서에서 현재 read/write scope, 버전, 품목별 공급가 설정 전제 확인.
- [ ] product_no/variant_code/shop_no의 정확한 관계와 금액 제약 확인.
- [ ] 테스트몰에서 한 품목 조회→변경→재조회→복원을 수행할 절차 준비.
- [ ] 다른 필드가 바뀌지 않는지 before/after 대조.
- [ ] 호출 제한·429·일시 오류·삭제된 품목·권한 철회 동작 확인.
- [x] OAuth state·launch 서명·전체 query 보존, 토큰 격리·refresh 잠금 구현. — `src/lib/cafe24/{oauth,launch,token-store}.ts`, `src/proxy.ts` (2026-09-17)

## 7. 단계별 작업

### A. 2시간 데모

- [x] 20분: 합성 목록·정상/오류 CSV와 데이터 모델. — `src/fixtures/supply-price-import.ts`, `src/features/imports/model.ts`
- [x] 40분: 검증·정확 매칭·변경 계산. — `src/features/imports/csv.ts`, `validate.ts`
- [x] 40분: 미리보기·행별 오류·명세 파일 출력. — `src/features/imports/preview.ts`, `src/app/demo/page.tsx`
- [x] 20분: 중복·빈값·0·미매칭·동일값 사례 검증. — 단위 테스트 50개, 브라우저 스모크 25개

완료: 1,000행 이하 파일에서 모든 행의 판정이 설명되고 오류가 있는 작업은 확정 불가. 실제 변경은 하지 않는다.
검증: `npm run verify` (lint → typecheck → test 50 → build → smoke 25) 통과. 상세는 `README.md`, `QA.md`.

### B. 한 품목 쓰기 검증 — 다음 상한 선기록

- [x] OAuth 콜백·토큰 교환 스캐폴드. — `src/lib/cafe24/{env,oauth,token-store}.ts`, `src/app/api/cafe24/oauth/{start,callback}/route.ts`, 단위 테스트 15개, E2E 4개 (2026-09-17)
- [ ] 앱/권한·테스트몰 전제 확인, 한 품목 before snapshot.
- [ ] 쓰기·재조회·복원 adapter와 실패 시나리오 구현.
- [ ] 읽기/쓰기 제약으로 불가능하면 원가 보고서 등 다른 제품으로 자동 전환하지 않고 결과 기록.

### C. 작은 배치 파일럿

- [ ] 실제 익명 원가표와 반복 변경 사례 확인.
- [ ] 소규모 확정 배치, 작업 재개·충돌·부분 실패 검증.
- [ ] 입력 준비시간·지원시간·오류·실제 반영 건수 기록.
- [ ] 반복 필요에 따라 독립 앱/기존 앱 확장/일회성 가져오기 중 오퍼 선택.

## 8. 검증과 다음 작업

필수 테스트: 앞자리 0, 중복 코드, 0과 빈값, 다른 몰 같은 코드, 수치 정밀도, 미리보기 후 외부 변경, timeout 후 이미 반영, worker 중단·재개, 중복 실행, 복원 전 외부 수정, 삭제된 품목. mock adapter로 실패를 재현하고 live QA는 `QA.md`에 기대 결과·설치 한도 소모 여부를 적어 사용자 수동 검증 절차로 진행한다.

다음은 A. 시작 시 llm-wiki 탐색 카드에 날짜·2시간 상한·담당 기록. API 문서상 가능성은 수요 증거가 아니며, 우리 오퍼의 노출·결제는 미측정이다. 새 API 권한·배포·가격·지속 투자 게이트는 아직 확정하지 않았다.
